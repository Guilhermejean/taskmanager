📋 Task Manager API

API REST desenvolvida em Java com Spring Boot, focada em boas práticas de arquitetura, organização em camadas, padrões de projeto e persistência com JPA.

Projeto ideal para portfólio profissional (GitHub / LinkedIn).

🎯 Objetivo do Projeto

Construir uma API REST de gerenciamento de tarefas, aplicando conceitos amplamente utilizados no mercado, como:

Arquitetura em camadas

Separação de responsabilidades

DTOs para entrada e saída de dados

Persistência com Spring Data JPA

Padrões de Projeto:

Factory

Strategy

API testável via Insomnia / Swagger UI

Código limpo e organizado

🛠️ Tecnologias Utilizadas

Java 21+

Spring Boot 3.x / 4.x

Maven

Spring Web

Spring Data JPA

Hibernate

Banco de dados H2 (em memória)

Swagger / OpenAPI

Insomnia (testes da API)

📌 (Futuro) PostgreSQL

📦 Estrutura de Pacotes
com.jean.taskmanager
 ├── controller
 ├── service
 ├── repository
 ├── model
 ├── dto
 ├── factory
 ├── strategy
 ├── exception
 └── config

📁 controller

Responsável por receber e responder às requisições HTTP.

Define endpoints REST

Converte DTOs

Não contém regra de negócio

@RestController
@RequestMapping("/tarefas")
public class TarefaController { }

📁 service

Camada de regra de negócio.

Criação de tarefas

Edição

Conclusão

Cancelamento

Integração com Factory e Strategy

📁 repository

Camada de persistência usando Spring Data JPA.

public interface TarefaRepository extends JpaRepository<Tarefa, Long> { }

📁 model

Representa o domínio da aplicação.

Entidades JPA

Enums de domínio

Entidade principal
@Entity
@Table(name = "tarefas")
public class Tarefa {
    private String titulo;
    private Prioridade prioridade;
    private StatusTarefa status;
    private LocalDate prazo;
}

📁 dto (Data Transfer Object)

Camada responsável por entrada e saída de dados da API.

DTOs existentes:

TarefaRequestDTO → criação

TarefaUpdateDTO → edição

TarefaResponseDTO → resposta da API

Evita expor entidades diretamente.

📁 factory

Implementa o padrão Factory.

Responsável por:

Criar tarefas

Aplicar automaticamente a Strategy correta baseada na prioridade

Tarefa tarefa = factory.criar(titulo, descricao, prioridade);

📁 strategy

Implementa o padrão Strategy.

Define regras diferentes de negócio conforme a prioridade da tarefa:

ALTA

MEDIA

BAIXA

PADRAO

Exemplo:

Prazo automático

Comportamentos diferentes por prioridade

📁 exception

Centraliza o tratamento de erros da aplicação.

Exceções customizadas

Respostas HTTP padronizadas

Uso de @ControllerAdvice

📁 config

Configurações globais:

Swagger / OpenAPI

Beans

Configurações gerais

📌 Swagger não esteve disponível em algumas versões do Spring Boot durante o desenvolvimento, mas está previsto e configurado.

🔄 Fluxo da Aplicação
Requisição HTTP
   ↓
Controller
   ↓
Service
   ↓
Factory
   ↓
Strategy
   ↓
Repository (JPA)
   ↓
Banco de Dados (H2)

🚀 Endpoints Disponíveis
➕ Criar tarefa
POST /tarefas

{
  "titulo": "Estudar Spring Boot",
  "descricao": "Revisar JPA e padrões",
  "prioridade": "ALTA"
}

📄 Listar tarefas
GET /tarefas

✏️ Editar tarefa
PUT /tarefas/{id}

{
  "titulo": "Novo título",
  "descricao": "Nova descrição",
  "prioridade": "MEDIA"
}

✅ Concluir tarefa
PUT /tarefas/{id}/concluir

❌ Cancelar tarefa
PUT /tarefas/{id}/cancelar

▶️ Como Executar o Projeto
Pré-requisitos

Java instalado

Maven ou Maven Wrapper

Executar
./mvnw.cmd spring-boot:run


ou

mvn spring-boot:run

🧪 Testando a API
Insomnia

Criar requisições REST

Enviar JSON

Validar status HTTP

Ver respostas

Swagger UI
http://localhost:8080/swagger-ui/index.html

H2 Console
http://localhost:8080/h2-console


JDBC URL:

jdbc:h2:mem:testdb

💼 Sobre o Projeto

Este projeto foi desenvolvido com foco em:

Código limpo

Arquitetura real de mercado

Padrões de projeto

Boas práticas com Spring Boot

Ideal para demonstrar conhecimento em:

Java

Spring Boot

APIs REST

JPA / Hibernate

Design Patterns

📌 Projeto em evolução — novas funcionalidades e melhorias contínuas serão adicionadas.